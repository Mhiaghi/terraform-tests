events {}

http {
    server {
        listen 80;

        location / {
            root /usr/share/nginx/html;
            index index.html;
            try_file $uri $uri.html $uri/ =404;
        }

        location /api/ {
            proxy_pass http://backend:5000/;
        }
    }
}